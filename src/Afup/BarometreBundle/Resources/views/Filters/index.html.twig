{% form_theme form "AfupBarometreBundle:Filters:theme.html.twig" %}

{{ form_start(form) }}
    {{ form_errors(form) }}

    {% for child in form %}
        {{ form_row(child, { 'widget_attr': {'class' : 'hide'}}) }}
    {% endfor %}

    {{ form_rest(form) }}
{{ form_end(form) }}
