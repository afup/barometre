{% extends 'knp_menu.html.twig' %}

{% block list %}
    {% set listAttributes = listAttributes|merge({class: 'nav nav-pills nav-stacked'}) %}
    {{ parent() }}
{% endblock %}

{% block item %}
    {% set options = options|merge({currentClass: 'active'}) %}
    {{ parent() }}
{% endblock %}

{% block label item.label|trans %}
